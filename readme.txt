PHP-2. Урок 5.
Домашняя работа.

1. Добавили в проект класс исключений, возникающих при работе с базой данных (файл DbException.php).
 Бросили исключения в случаях отсутствия соединения с БД, ошибки запроса.

2. Ловим исключения во фронт-контроллере и выводим страницу об ошибке (шаблон с сообщениемоб ошибке находится в template файл error.php).

3. Добавили исключение "Ошибка 404 - не найдено" (в App/ файл Error404Exception.php). Ловим исключение во фронт-контроллере.

4. Добавили в базовую модель метод fill(array $data), который заполняет свойства модели данными из массива, валидируя их.
 Применили паттерн "Мультиисключение" (в App/ файл MultiException.php).

5. * Добавили в проект класс-логгер (в App/Models/ файл Logger.php).
 Он записывает в текстовый файл log.txt информацию об ошибках - когда, где и какая ошибка возникла